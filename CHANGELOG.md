# Changelog

## 0.1.0 - Release candidate

- Adds a local-first `containerghost` CLI for scanning devcontainer, Docker Compose, Dockerfile, package script, and `.env.example` drift.
- Ships Markdown and JSON renderers with deterministic timestamps for repeatable review evidence.
- Includes a fixture-backed smoke path in `examples/basic` and package verification through `npm run release:check`.

## Notes

- No package has been published from this repository yet.
- Future entries should record user-visible CLI, report schema, and packaging changes.
